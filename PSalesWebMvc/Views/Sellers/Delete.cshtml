@model PSalesWebMvc.Models.Seller
@{
    ViewData["Title"] = "Delete";
}

<h2>@ViewData["Title"]</h2>
<h3>Are your sure you want to delete this?</h3>

<div>
    <h4> Seller</h4>
    <hr />
    <dl class="dl-horizontal">
        <!--lista os elementos-->
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.BirthDate)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.BirthDate)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.BaseSalary)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.BaseSalary)
        </dd>
    </dl>
   <!--cria a ação de confirmação de -->
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" /><!--envia o formulário(é um campo oculto)-->
        <input type="submit" value="Delete" class="btn btn-danger" /><!--danger confirma a deleção-->
        <a asp-action="Index">Back to List</a>
    </form>
</div>
